name: Run fizzbuzz.exe

on:
  push:
  workflow_dispatch:

jobs:
  run:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run fizzbuzz.exe
        shell: pwsh
        run: |
          .\fizzbuzz.exe
